{
  "name": "bcr-app",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm run start --workspace=@bcr-app/mobile",
    "api": "npm run dev --workspace=@bcr-app/api",
    "install:all": "npm install && npm install --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
